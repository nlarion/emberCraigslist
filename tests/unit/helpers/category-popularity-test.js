import { categoryPopularity } from 'super-lists/helpers/category-popularity';
import { module, test } from 'qunit';

module('Unit | Helper | category popularity');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = categoryPopularity([42]);
  assert.ok(result);
});
