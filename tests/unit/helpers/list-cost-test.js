import { listCost } from 'super-lists/helpers/list-cost';
import { module, test } from 'qunit';

module('Unit | Helper | list cost');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = listCost([42]);
  assert.ok(result);
});
